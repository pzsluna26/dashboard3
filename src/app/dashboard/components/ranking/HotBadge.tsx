'use client';

export default function HotBadge() {
  return (
    <div className="px-1 py-0.5 bg-gradient-to-r from-red-500 to-orange-500 text-white text-xs font-bold rounded animate-pulse">
      HOT
    </div>
  );
}